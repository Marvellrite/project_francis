import { Context } from "../components/Calculate";
import { useContext } from "react";

const useContext_1 = ()=>{

    const context = useContext(Context);
    return context;
}

export default useContext_1